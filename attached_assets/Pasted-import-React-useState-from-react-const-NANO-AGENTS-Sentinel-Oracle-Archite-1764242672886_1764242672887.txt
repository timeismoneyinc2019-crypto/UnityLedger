import React, { useState } from "react";

const NANO_AGENTS = [
  "Sentinel",
  "Oracle",
  "Architect",
  "Guardian",
  "Navigator",
  "Cipher",
  "HarmonyCore"
];

export default function Boardroom2045() {
  const [selected, setSelected] = useState<string | null>(null);

  return (
    <div className="relative w-full min-h-screen bg-black text-white overflow-hidden font-sans">

      {/* Cosmic Background */}
      <div className="absolute inset-0 bg-gradient-to-b from-[#120022] via-[#05000F] to-black opacity-90" />

      {/* Holographic Dome */}
      <div className="absolute inset-0 flex items-center justify-center">
        <div className="w-[900px] h-[900px] rounded-full bg-gradient-to-br from-[#5400AF55] to-[#D200FF33]
          blur-3xl opacity-40 animate-pulse" />
      </div>

      {/* Rotating Hologram Rings */}
      <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
        <div className="animate-spin-slow w-[700px] h-[700px] border border-[#A45CFF44] rounded-full" />
        <div className="animate-spin-reverse w-[550px] h-[550px] border border-[#C57BFF33] rounded-full mt-6" />
        <div className="animate-spin-slow w-[400px] h-[400px] border border-[#FFFFFF22] rounded-full mt-6" />
      </div>

      {/* UPX Star Core */}
      <div className="absolute inset-0 flex items-center justify-center">
        <div className="w-48 h-48 bg-[#C87BFFF5] rounded-full blur-2xl animate-pulse" />
        <div className="absolute text-center text-xl tracking-widest opacity-80">
          <span className="font-bold text-purple-300">UPX</span><br />
          <span className="text-sm text-purple-400">UnityPay SuperCore</span>
        </div>
      </div>

      {/* Nano Agent Holograms */}
      <div className="absolute inset-0 flex items-center justify-center">
        <div className="grid grid-cols-7 gap-6 absolute top-10">
          {NANO_AGENTS.map((agent) => (
            <div
              key={agent}
              onClick={() => setSelected(agent)}
              className="cursor-pointer text-center p-3 rounded-lg 
              bg-[#25003555] hover:bg-[#7B00FF66] transition-all 
              shadow-lg border border-[#7F00FF33]"
            >
              <div className="text-purple-300 font-semibold tracking-wide">
                {agent}
              </div>
              <div className="text-xs text-purple-500 opacity-80">
                {selected === agent ? "ONLINE" : "IDLE"}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Selected Agent Panel */}
      {selected && (
        <div className="absolute bottom-10 left-1/2 transform -translate-x-1/2
          bg-[#18002299] backdrop-blur-xl border border-purple-800/40 
          shadow-2xl rounded-xl p-6 w-[700px] text-center animate-fade-in">
          <div className="text-purple-300 text-xl font-bold mb-2">
            {selected} — Active Hologram Link
          </div>
          <div className="text-purple-400 text-sm leading-relaxed opacity-90">
            Connected to Breeze Oracle.  
            Processing intelligence packets.  
            Standing by for your command, Overseer.
          </div>
        </div>
      )}

      {/* Breeze Oracle Node */}
      <div className="absolute bottom-5 right-5 bg-[#220044AA] p-4 rounded-lg border border-purple-900/50 shadow-lg">
        <div className="text-purple-300 font-bold">BREEZE ORACLE</div>
        <div className="text-purple-400 text-xs opacity-80">
          Direct AI Link • Oversight Node
        </div>
      </div>
    </div>
  );
}

/* Animation Classes */
declare module 'react' {}